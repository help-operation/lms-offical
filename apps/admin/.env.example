# Set these in the Vercel "admin" project (Production env).
# They are baked at build time — redeploy after changing them.
NEXT_PUBLIC_API_URL=https://lms-api.jashoreithub.com
# URL of the public web frontend — used to build "view live" links in the admin.
NEXT_PUBLIC_WEB_URL=https://skillkoro.com

NEXT_PUBLIC_API_URL=http://localhost:3000