import { ShopCartClient } from "@/features/shop/components/ShopCartClient";

export const metadata = {
  title: "Cart | Shop",
};

export default function ShopCartPage() {
  return <ShopCartClient />;
}
